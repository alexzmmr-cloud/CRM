# CRM-lite для агентства выставочных стендов

Короткое описание: система учёта и ведения входящих лидов из разных каналов (сайт, почта, телефон, рекомендации, ручной ввод) с последующей квалификацией, привязкой к компаниям и контактным лицам, фиксацией параметров проекта (бюджет, площадка, сроки, формат) и ведением сделок по воронке до закрытия (успех или отказ). Помогает менеджерам не терять заявки, отслеживать этапы сделок, контролировать задачи и дедлайны, видеть ответственных лиц у клиента и оценивать текущий объём потенциальной выручки в работе.

## Основные возможности

* Сущности: Lead, Account, Contact, Opportunity, Stage, Activity
* Приём и учёт входящих лидов с обязательным источником (`site`, `email`, `phone`, `referral`, `manual`)
* CRUD для лидов, компаний, контактов и сделок; списки и карточки сущностей
* Упрощённый convert lead — квалификация лида и привязка к компании и контактному лицу, при необходимости создание сделки
* Связи между компаниями, контактами, сделками и активностями, видимые в карточках сущностей
* Фиксация параметров сделки: бюджет, площадка, сроки, формат
* Воронка сделок по стадиям с правилами переходов в won и lost
* Activity как note и task; task с dueDate и состоянием done
* Быстрые действия: создать сделку, сменить стадию, добавить note, добавить task, выполнить convert lead
* Поиск и фильтры
* Отдельный экран Dashboard с показателями текущего состояния базы (объём потенциальной выручки в работе)

## Основной пользовательский сценарий

1. Пользователь запускает проект.
2. Подготавливает данные.
3. Создаёт лида в интерфейсе.
4. Конвертирует лида в контакт и компанию, при необходимости создаёт сделку.
5. Открывает карточки связанных сущностей и видит корректные связи.
6. Двигает сделку по воронке.
7. Добавляет заметку и задачу.
8. Проверяет поиск и фильтры.
9. Открывает отдельный Dashboard и видит показатели из текущего состояния базы.
10. Меняет лида или сделку и проверяет, что Dashboard обновился через повторный запрос.

## Стек и инструменты

* Next.js, React, TypeScript
* PostgreSQL
* Prisma — `prisma@6.19.3`, `@prisma/client@6.19.3` (стандартный контур Prisma 6: `DATABASE_URL` читается в `schema.prisma` через `env("DATABASE_URL")`; `@prisma/adapter-pg`, `PrismaPg` и `prisma.config.ts` не используются)
* Zod — валидация данных
* chart.js@4.5.1 и react-chartjs-2@5.3.1 — диаграммы на Dashboard

## Структура проекта

Требует уточнения — на момент написания README проект не содержит файлов, кроме `.gitignore`.

## Запуск

Команда запуска пока не определена — требует уточнения.

## Полезные команды

Требует уточнения — на момент написания README в проекте нет настроенных скриптов (тесты, линтер, сборка, деплой).

## Что важно знать при работе с проектом

* Проект находится на начальном этапе: репозиторий переинициализирован с нуля, кода ещё нет.
* Работа ведётся по шагам: README → план → итерации, каждый шаг — по явному подтверждению.
