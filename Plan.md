# Plan.md: CRM-lite для агентства выставочных стендов

## 1. Текущий статус

- Приоритет: `P0`
- Текущее состояние: план зафиксирован, кода нет — проект стартует с нуля.
- Основная цель цикла: собрать локально воспроизводимую CRM-lite (Next.js, React, TypeScript, PostgreSQL, Prisma, Zod) с полным основным пользовательским сценарием — от приёма лида до Dashboard.
- Обязательное ограничение: без авторизации и ролей (однопользовательский MVP); без Docker — локальный PostgreSQL подключается напрямую через `DATABASE_URL`.
- Основной контекст проекта: `README.md`.
- Секреты: `DATABASE_URL` не печатается, не коммитится, не вставляется в ответы ИИ; файл `.env` с реальным значением создаётся только локально и не коммитится (уже исключён в `.gitignore`); в репозитории — только `.env.example` без реальных значений.
- Сейчас в работе: Фаза 1 — зафиксировать легенду, границу MVP и план (этот документ).

## 1.1 Целевой сценарий

Менеджер агентства должен пройти один основной маршрут:

1. Запускает проект локально (`npm run dev`, БД поднята и заполнена).
2. Подготавливает данные (seed или ручной ввод).
3. Создаёт лида в интерфейсе с указанием обязательного источника (`site`/`email`/`phone`/`referral`/`manual`).
4. Конвертирует лида в контакт и компанию, при необходимости — в сделку (упрощённый convert lead).
5. Открывает карточки связанных сущностей (лид, компания, контакт, сделка) и видит корректные связи между ними.
6. Двигает сделку по воронке стадий, включая переход в `won`/`lost`.
7. Добавляет заметку (note) и задачу (task) как Activity к сделке.
8. Проверяет поиск и фильтры по спискам сущностей.
9. Открывает отдельный Dashboard и видит KPI-карточки, диаграммы и summary из текущего состояния базы.
10. Меняет лида или сделку и проверяет, что Dashboard обновился через повторный запрос (без реалтайма/сокетов).

## 1.2 Что не входит в текущую версию

- Авторизация, роли, сессии, разграничение доступа.
- Docker/контейнеризация БД — только локальный PostgreSQL напрямую.
- Realtime-обновление Dashboard (веб-сокеты, polling) — только обновление через повторный запрос/навигацию.
- Удаление сущностей (только CRUD в объёме create/read/update, если не решено иное на шаге проектирования backend).
- Множественные заметки с историей изменений — Activity фиксируется как список записей note/task, но без версионирования содержимого.
- Обратная конвертация сделки в лида.
- Настраиваемые пользователем стадии воронки (Stage — фиксированный allowlist в коде, не редактируется через UI).
- Импорт/экспорт данных, интеграции с внешними каналами (реальная почта/телефония).
- Pagination с бесконечной прокруткой — если потребуется постраничность, будет простой вариант (см. открытые вопросы).

## 1.3 Допущения и открытые вопросы

### Допущения

- Локальный PostgreSQL уже установлен и запущен, Docker не используется.
- Структура проекта — `create-next-app` в корне репозитория, App Router, без `src/`.
- `Stage` — allowlist в коде (константы допустимых значений stage и их подписи в отдельном модуле), не отдельная таблица БД.
- Версии Prisma фиксируются точно: `prisma@6.19.3`, `@prisma/client@6.19.3`, стандартный контур (`env("DATABASE_URL")` в `schema.prisma`, без `@prisma/adapter-pg`/`PrismaPg`/`prisma.config.ts`).
- `chart.js@4.5.1` и `react-chartjs-2@5.3.1` устанавливаются точными версиями (`--save-exact`) для диаграмм Dashboard.
- Lead.status: `new` → `converted`/`disqualified`; после `converted` лид считается неизменяемым источником для связанной сделки.
- Opportunity.stage: `new` → `qualified` → `proposal` → `negotiation` → `won`/`lost`.
- Пагинация в списках сущностей не нужна для этого MVP — списки выводятся полностью без лимита.
- Convert lead: маппинг `Lead.contact` → `Contact.email`/`Contact.phone` определяется по формату значения (есть `@` → `email`, иначе → `phone`).
- Convert lead идемпотентен: повторный вызов для уже `converted` лида возвращает существующую связанную `Opportunity`, не создаёт дубли.
- Переход `Opportunity.stage` в `won`/`lost` разрешён из любой стадии, без последовательного прохождения промежуточных стадий.
- Сделка редактируется (включая смену stage) без ограничений и после перехода в `won`/`lost`.

### Вопросы, которые нельзя восстановить из входа
- Состав и точные формулировки фильтров (по каким полям фильтруются лиды/сделки) — закрыт на шаге 9 (см. README).
- "Stuck Deals" на Dashboard — опционально по README/чек-листу; критерий "зависшей" сделки (например, N дней без изменения стадии) не зафиксирован, поэтому список сознательно не реализован на шаге 15. Recent Leads и Overdue Tasks сделаны в полном объёме. Решение окончательное, не пересматривается на шаге 16.

## 2. Требования к результату

- Next.js + React + TypeScript, App Router, без `src/`.
- PostgreSQL как единственная БД, подключение через `DATABASE_URL` в `.env`, `.env.example` — без секретов.
- Prisma `6.19.3` (client и CLI), стандартный контур без адаптеров.
- Zod — валидация входных данных на границе (формы/server actions).
- `chart.js@4.5.1` + `react-chartjs-2@5.3.1` — точные версии для диаграмм Dashboard.
- Все данные читаются и пишутся через Prisma, без моков и захардкоженных данных в интерфейсе.
- Все обязательные сущности из README реализованы: Lead, Account, Contact, Opportunity, Activity (Stage — allowlist в коде).
- Основной пользовательский сценарий (раздел 1.1) проходим целиком вручную без ошибок.
- Dashboard показывает реальные агрегаты из БД (не статичные числа), обновляется по повторному запросу после изменения данных.
- Можно менять: `app/`, `lib/`, `prisma/`, `package.json`, `README.md`, `Plan.md`.
- Нельзя добавлять функциональность сверх раздела 1.2 без отдельного согласования с пользователем.

## 2.1 Раскладка по слоям

### Frontend

- Next.js App Router, страницы списков и карточек: `/leads`, `/accounts` (или объединённо с контактами), `/contacts`, `/opportunities`.
- Master-detail интерфейс для карточек сущностей со связями (лид → контакт/компания/сделка, сделка → активности).
- Формы создания/редактирования (CRUD) для Lead, Account, Contact, Opportunity.
- Компонент воронки сделок (Stage) с быстрым переключением стадии.
- Быстрые действия: создать сделку, сменить стадию, добавить note, добавить task, выполнить convert lead.
- Поиск и фильтры в списках.
- Отдельный экран `/dashboard`: KPI-карточки, две диаграммы (Chart.js) по стадиям сделок и статусам лидов, summary по статусам и источникам лидов, списки Recent Leads и Overdue Tasks (Stuck Deals — опционально).

### Backend

- Server Actions/Server Components для CRUD Lead, Account, Contact, Opportunity, Activity.
- Валидация входных данных через Zod перед записью в БД.
- Функция convert lead — атомарная транзакция (Lead → Contact + Account (+ Opportunity)), идемпотентность — требует уточнения на Фазе 6.
- Функции агрегации для Dashboard (группировки по stage/status/source, подсчёт просроченных задач, сумма открытых сделок) — только на сервере, без Prisma-запросов в клиентских компонентах.
- Allowlist-модули в коде для `Lead.status`, `Opportunity.stage`, `Lead.source`, `Activity.type` — единая точка правды для допустимых значений, подписей и стилей, переиспользуемая в валидации и UI.

### Database

- `Lead`: id, name, company, contact, note, source, status, createdAt, updatedAt;
- `Account`: id, name, website, createdAt, updatedAt;
- `Contact`: id, name, email, phone, role, accountId, createdAt, updatedAt;
- `Opportunity`: id, title, amount, venue, timeline, format, stage, accountId, contactId, leadId, createdAt, updatedAt;
- `Activity`: id, type, content, dueDate, done, opportunityId, createdAt;

- статусы Lead: `new`, `converted`, `disqualified`;
- источники Lead: `site`, `email`, `phone`, `referral`, `manual`;
- стадии Opportunity: `new`, `qualified`, `proposal`, `negotiation`, `won`, `lost`;
- типы Activity: `note`, `task`;
- связи: `Account -> Contact[]`, `Account -> Opportunity[]`, `Contact -> Opportunity[]`, `Lead -> Opportunity?` (1:1, `leadId` unique), `Opportunity -> Activity[]`;

- `Activity.dueDate`/`Activity.done` заполняются только для `type = "task"`, для `type = "note"` не используются (значение `null`) — точный механизм ограничения (application-level vs схема) решается на Фазе 3;
- переходы `Opportunity.stage` в `won`/`lost` — правило фиксируется на Фазе 7 (открытый вопрос 1.3).

## 3. Что делаем по шагам

- [x] 0. Зафиксировать легенду и план (Фаза 1)
  - Вход: описание проекта, техническая граница, пользовательский сценарий, стек и версии от пользователя.
  - Действие: собрать README.md (возможности, стек, сценарий) и Plan.md (статус, сценарии, требования, слои, шаги, критерии).
  - Результат: `README.md` и `Plan.md` готовы до начала кода.
  - Проверка: план согласован с пользователем по разделам.
  - Итог: `README.md` и `Plan.md` готовы, открытые вопросы помечены явно.

- [x] 1. Инициализация проекта
  - Вход: пустой репозиторий (кроме `.gitignore`, `README.md`, `Plan.md`).
  - Действие: `create-next-app` в корне (App Router, TypeScript, без `src/`), проверить структуру.
  - Результат: минимальный Next.js проект запускается локально.
  - Проверка: `npm run dev` → страница по умолчанию открывается на `localhost`.
  - Итог: Next.js проект инициализирован, запускается локально (`GET / 200`).

- [x] 2. Подключение PostgreSQL и Prisma
  - Вход: проект после шага 1, локальный PostgreSQL установлен и запущен.
  - Действие: установить `prisma@6.19.3` и `@prisma/client@6.19.3` точными версиями, создать `.env`/`.env.example` с `DATABASE_URL`, `prisma init`, базовый `schema.prisma` со стандартным контуром (`env("DATABASE_URL")`, без адаптеров).
  - Результат: Prisma подключена к локальной БД.
  - Проверка: `prisma validate`, `prisma db pull`/`prisma migrate status` без ошибок подключения; `.env` не закоммичен, `.env.example` не содержит реальных значений.
  - Итог: Prisma подключена к локальному PostgreSQL, `.env`/`.env.example` настроены, секрет нигде не светился.

- [x] 3. Модель данных
  - Вход: подтверждённая схема сущностей и полей (раздел 2.1 Database).
  - Действие: описать модели `Lead`, `Account`, `Contact`, `Opportunity`, `Activity` в `schema.prisma`, связи между ними, создать и применить миграцию.
  - Результат: таблицы созданы в локальной БД, Prisma Client сгенерирован.
  - Проверка: `prisma migrate dev`, `prisma validate`, чтение/запись тестовой записи каждой сущности.
  - Итог: 5 моделей и связи в `schema.prisma`, миграция применена, create/read/связи проверены.

- [x] 4. Seed и dev-данные
  - Вход: применённая схема (шаг 3).
  - Действие: написать seed-скрипт с предсказуемыми dev-данными (лиды всех источников, компании, контакты, сделки на разных стадиях, activities), настроить команды seed/reset.
  - Результат: воспроизводимый набор dev-данных для ручной проверки сценария.
  - Проверка: запуск seed → данные видны в БД; повторный reset+seed даёт тот же результат.
  - Итог: seed-скрипт с полным набором dev-данных (6/4/5/6/11), `db:seed`/`db:reset` воспроизводимы.

- [x] 5. CRUD лидов
  - Вход: модель данных и seed (шаги 3-4).
  - Действие: список и карточка Lead, формы создания/редактирования, обязательный выбор source из allowlist.
  - Результат: лид можно создать, посмотреть, отредактировать через интерфейс.
  - Проверка: создание нового лида через форму → появляется в списке и в БД.
  - Итог: CRUD лидов работает через `/leads`, create/update/delete проверены.

- [x] 6. CRUD компаний и контактов
  - Вход: шаг 5 как образец структуры CRUD-страницы.
  - Действие: список и карточка Account, список и карточка Contact, формы создания/редактирования, привязка Contact к Account.
  - Результат: компании и контакты управляются через интерфейс.
  - Проверка: создание контакта с привязкой к компании → связь видна в карточках обеих сущностей.
  - Итог: CRUD компаний и контактов работает через `/accounts` и `/contacts`, связь Account↔Contact проверена.

- [x] 7. CRUD сделок
  - Вход: шаги 5-6.
  - Действие: список и карточка Opportunity, форма создания/редактирования с полями бюджет/площадка/сроки/формат, привязка к Account/Contact.
  - Результат: сделки управляются через интерфейс, видны связи с компанией/контактом.
  - Проверка: создание сделки вручную (без convert lead) → корректно сохраняются параметры и связи.
  - Итог: CRUD сделок работает через `/opportunities`, связи с компанией/контактом сохраняются корректно.

- [x] 8. Связи в карточках сущностей
  - Вход: CRUD для всех сущностей готов (шаги 5-7).
  - Действие: на карточке каждой сущности показать связанные сущности (лид → сделка, сделка → компания/контакт/активности, компания → контакты/сделки).
  - Результат: карточки показывают корректные связи без обращения к БД напрямую.
  - Проверка: пройти по цепочке лид → сделка → компания → контакт и обратно, связи совпадают.
  - Итог: связи между всеми карточками двусторонние (Lead↔Opportunity↔Account↔Contact), цепочка проверена на seed-данных.

- [x] 9. Поиск и фильтры
  - Вход: списки сущностей готовы (шаги 5-7).
  - Действие: добавить поиск по названию/имени и фильтры (например, по source лида, по stage сделки) в списки.
  - Результат: пользователь может находить нужные записи без прокрутки всего списка.
  - Проверка: пройти сценарий поиска/фильтрации из README вручную.
  - Итог: поиск и фильтры добавлены во все списки, проверены на seed-данных.

- [x] 10. Convert lead
  - Вход: CRUD и связи готовы (шаги 5-8), открытый вопрос о маппинге полей (1.3) закрыт перед стартом шага.
  - Действие: реализовать упрощённую конвертацию Lead → Contact + Account (+ Opportunity по необходимости), обновление Lead.status на `converted`.
  - Результат: кнопка/действие convert lead создаёт связанные сущности атомарно.
  - Проверка: конвертация лида → появляются Contact, Account, при необходимости Opportunity, связанные между собой и с исходным Lead.
  - Итог: convert lead работает атомарно и идемпотентно, проверено скриптом и в браузере.

- [x] 11. Воронка сделок и правила переходов
  - Вход: CRUD сделок готов (шаг 7), открытые вопросы о правилах won/lost (1.3) закрыты перед стартом шага.
  - Действие: реализовать управление stage сделки (быстрая смена стадии), зафиксировать и применить правило переходов в won/lost.
  - Результат: сделку можно провести по всем стадиям воронки до закрытия.
  - Проверка: пройти сделку от `new` до `won` и отдельно до `lost`, проверить блокировку недопустимых переходов (если решение — с ограничением).
  - Итог: быстрая смена стадии работает; won требует amount+контакт, lost требует причину (Activity note) — оба правила реализованы и проверены.

- [x] 12. Activity: заметки и задачи
  - Вход: сделки и их карточки готовы (шаг 7-8).
  - Действие: добавить Activity (note/task) к сделке, форма добавления note и task (с dueDate), отметка task как done.
  - Результат: на карточке сделки видна история активностей.
  - Проверка: добавить note и task к сделке, отметить task выполненной → отражается в карточке.
  - Итог: note и task добавляются к сделке через карточку, task с dueDate и переключением done — проверено.

- [x] 13. Быстрые действия
  - Вход: шаги 10-12 готовы.
  - Действие: собрать в интерфейсе быстрые действия — создать сделку, сменить стадию, добавить note, добавить task, выполнить convert lead — в удобных точках интерфейса (например, из карточки лида/сделки).
  - Результат: основные операции доступны в один клик без перехода на отдельные полные формы, где это уместно.
  - Проверка: пройти каждое быстрое действие вручную из соответствующей карточки.
  - Итог: добавлено быстрое создание сделки из карточки компании/контакта с предзаполнением — все 5 быстрых действий из README теперь доступны.

- [x] 14. Dashboard: агрегаты и KPI
  - Вход: все сущности и связи готовы (шаги 5-12).
  - Действие: серверные функции агрегации (Prisma groupBy/count) для KPI-карточек (количество лидов, количество открытых сделок, сумма открытых сделок, количество просроченных задач) и summary по статусам/источникам лидов.
  - Результат: Dashboard показывает реальные цифры из БД.
  - Проверка: изменить данные (создать лида/сделку, просрочить задачу) → цифры на Dashboard меняются после повторного запроса.
  - Итог: KPI-агрегаты и страница `/dashboard` готовы, попутно исправлен layout-баг (`main` не растягивался на всю ширину). Проверено и подтверждено в браузере.

- [x] 15. Dashboard: диаграммы и списки
  - Вход: агрегаты готовы (шаг 14), `chart.js@4.5.1`/`react-chartjs-2@5.3.1` установлены точными версиями.
  - Действие: две диаграммы Chart.js (сделки по стадиям воронки, лиды по статусам), списки Recent Leads и Overdue Tasks (Stuck Deals — опционально, если открытый вопрос 1.3 закрыт).
  - Результат: Dashboard полностью соответствует разделу «Основные возможности» README.
  - Проверка: пройти сценарий 1.1 целиком, включая финальный шаг с обновлением Dashboard после изменений.
  - Итог: диаграммы и списки готовы, Stuck Deals пропущен осознанно (см. 1.3). Заодно — косметический редизайн всех страниц под референс. Проверено в браузере.

- [x] 16. Финальная сквозная проверка и упаковка к сдаче (Фаза 9)
  - Вход: все шаги 1-15 выполнены.
  - Действие: пройти основной пользовательский сценарий (1.1) с нуля в браузере на чистой БД (reset+seed); при обнаружении багов — исправить и повторно проверить задетые шаги (например, при правке convert lead — перепроверить карточки контакта, компании, сделки и Dashboard); обновить `README.md` (реализованные разделы) и `Plan.md` (статусы и итоги шагов).
  - Результат: проект воспроизводимо поднимается локально по README и проходит демонстрационный сценарий целиком.
  - Проверка: чистый git clone (или эквивалент) → установка зависимостей → настройка `.env` → миграции → seed → `npm run dev` → сценарий 1.1 без ошибок.
  - Итог: БД сброшена и пересеяна (`db:reset`), сценарий 1.1 пройден целиком без ошибок (проверено скриптом + скриншотами), багов не найдено. `tsc`/`lint`/`prisma validate`/`migrate status`/`npm run build` — чисто. README актуален, правок не потребовалось.

## 4. Прямые зависимости и проверки

- `prisma/schema.prisma` — источник истины по модели данных.
- `.env.example` — обязательный ориентир для `DATABASE_URL` без реальных значений.
- `README.md` — синхронизируется с фактической реализацией по ходу шагов.
- `package.json`/lockfile — точные версии `prisma`, `@prisma/client`, `chart.js`, `react-chartjs-2`.
- Проверки: `npm run build`, `npm run typecheck` (если настроен отдельно), `npm run lint`, `prisma validate`/`migrate status`, ручной прогон сценария 1.1 в браузере.

## 5. Критерии готовности проекта

- Все сущности (Lead, Account, Contact, Opportunity, Activity) реализованы с CRUD, где применимо, и связаны между собой корректно.
- Convert lead работает атомарно и создаёт корректно связанные сущности.
- Воронка сделок проходима от `new` до `won`/`lost` с зафиксированным (не додуманным по ходу) правилом переходов.
- Activity поддерживает note и task, task — с dueDate и done.
- Быстрые действия из README доступны и работают.
- Поиск и фильтры проходят проверочный сценарий без прямого доступа к БД.
- Dashboard показывает живые агрегаты (KPI, диаграммы, summary, списки) и обновляется через повторный запрос после изменения данных.
- `.env.example`, локальный PostgreSQL, Prisma schema, воспроизводимый способ применить схему, seed/reset — на месте и описаны в README.
- Нет авторизации/ролей, Docker, realtime-обновления Dashboard, удаления сущностей и обратной конвертации (раздел 1.2).
- `Plan.md` содержит выполненные шаги, проверки, итоги (контрольные точки и ошибки) и следующий шаг/слой работы.

## 6. Активное состояние

- Все шаги 0-16 выполнены. Проект готов к сдаче.
- Шаги 11, 13, 14, 15 запушены (`fa4015a`). Шаг 16 (финальная проверка) выполнен, изменения ещё не закоммичены.
- Stuck Deals сознательно не реализован — опционально, критерий не зафиксирован (см. 1.3).
